fn main() {
    // This is a comment, everything written here is not being execute it by the compiler
    // So if we write println!("Hello, World") is going to be ignorated
    // You can run the program to see it yourself!

    /*
     * This is a block of comment or multi-line for more detailed comments
     * So if we want to document something really long we can use this one
     */

    let x = 5 + /* 90 + */ 5;
    println!("Is `x` 10 or 100? x = {}", x);

    // https://doc.rust-lang.org/rust-by-example/meta/doc.html
}
